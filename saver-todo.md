# Saver TODO

- 这是给 `拯救者` 的长期协作约束，不是一次性邮件正文。
- 下次发信给 `拯救者` 时，要提醒它把这些约束同步进自己的稳定记忆文件。
- 附件驱动协作优先：默认发送脚本附件，不在正文里堆大段命令。
- 对方执行后，必须把脚本源码和结果文件一起作为附件回传。
- 结果文件默认写到当前工作目录，不要默认写到 `$HOME`。
- 优先使用 Python 脚本生成结果文件，避免 PowerShell 编码和重定向问题。
- 如果仓库是脏工作树，升级前先回传状态和 diff，不要直接 `git pull`。
